
import { Writer } from 'steno'

module.exports = Writer;

//export default async function createWriter(filePath, initData) {
//    const file = new Writer(appRootPath + filePath)
//    await file.write(JSON.stringify(initData));
//    return async function writedb(data) {
//        await file.write(JSON.stringify(data));
//    }
//}